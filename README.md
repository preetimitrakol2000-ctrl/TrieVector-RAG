# TrieVector-RAG // Prefix-Constrained Vector Matcher

A hybrid retrieval engine combining Trie-based string matching with vector similarity to restrict search domains before executing floating-point vector comparisons.

## Run Test
```bash
python trie_rag_app.py
